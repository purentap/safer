name: FusionLSTMModel

params:
  # Model architecture parameters
  img_dim: ${dataset.image_embedding_dim}
  action_dim: ${dataset.hidden_feature_dim}
  lstm_hidden_dim: 256
  gate_hidden_dim: 256
  use_gate: true  # Enable adaptive learnable weights
  output_dim: 1

