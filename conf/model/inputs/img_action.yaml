# @package _global_
inputs:
  name: img_action
  use_image: true
  use_action: true
  
  # Input dimensions
  img_dim: ${dataset.image_embedding_dim}
  action_dim: ${dataset.hidden_feature_dim}
  total_dim: ${dataset.total_input_dim}  # defined per-dataset

