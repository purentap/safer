# @package _global_
inputs:
  name: img_only
  use_image: true
  use_action: false
  
  # Input dimensions
  img_dim: ${dataset.image_embedding_dim}
  action_dim: 0  # Not used
  total_dim: ${dataset.image_embedding_dim} # For models that need single input_dim

